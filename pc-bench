#!/bin/bash
# Quick benchmark runner

set -e

echo "🚀 Running benchmarks..."

# Build in release mode
cargo build --release --bin benchmark --quiet

# Run benchmarks
cargo run --release --bin benchmark

echo ""
echo "For more options:"
echo "  pc-bench 10000     # Specific body count"
echo "  pc-bench full      # Run full benchmark suite"